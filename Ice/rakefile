
TARGET = 'Ice'

INCLUDE = ["#{ENV['OSPL_HOME']}/include/dcps/C/SAC", "#{ENV['OSPL_HOME']}/include", "#{ENV['OSPL_HOME']}/include/sys"]
LIBPATH = ["#{ENV['OSPL_HOME']}/lib"]
LIB = ["ddskernel", "dcpssac"]

require "#{ENV['CORTEX_HOME']}/build/package"
